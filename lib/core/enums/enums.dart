// ignore_for_file: constant_identifier_names

enum AppImageType {
  SVG,
  IMAGE,
  NOT_IMAGE,
}
enum AppImageUrlType {
  NETWORK,
  ASSET,
  FILE,
  ICON,
  IMAGE_CIRCLE,
}
enum AppButtonType {
  DEFAULT,
  OUTLINE,
}
enum AppPickerDate {
  MATERIAL,
  CUPERTINO,
}
enum AppInputType {
  TEXT,
  PASSWORD,
  NUMBER,
  DOUBLE,
  PRICE,
  EMAIL,
  PHONE,
  INCREMENT,
  MULTILINE,
  DATE,
}
enum TypeOfAlert {
  ERROR,
  SUCCESS,
  INFO,
  WARRING,
}
enum SocialType {
  NONE,
  GOOGLE,
  FACEBOOK,
  SLACK,
  MICROSOFT,
}